# ============================================
# CRITICAL: Environment Variables Required
# ============================================

# AI & Machine Learning
GROQ_API_KEY=gsk_your_groq_api_key_here
# Get free API key: https://console.groq.com/keys
# Required for: CrewAI agents, strategy generation

SERPAPI_KEY=your_serpapi_key_optional
# Get API key: https://serpapi.com/manage-api-key (Optional)
# Used for: Real SEO keyword research (Pro tier feature)
# If not set: Uses demo keyword data

# ============================================
# Payment & Revenue
# ============================================

STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
# Get from: https://dashboard.stripe.com/test/apikeys
# Production: Use sk_live_ key instead of sk_test_
# Required for: Pro tier checkout ($29/mo subscriptions)

STRIPE_PRICE_ID=price_your_stripe_price_id
# Create product: https://dashboard.stripe.com/test/products
# Set to: $29/month recurring subscription
# Copy the Price ID (starts with "price_")

STRIPE_WEBHOOK_SECRET=whsec_your_webhook_signing_secret
# CRITICAL: Required for auto-upgrade after payment
# Setup: Dashboard → Developers → Webhooks → Add endpoint
# Endpoint URL: https://your-api.com/api/stripe/webhook
# Events: Select "checkout.session.completed"
# Copy signing secret after creating webhook

# ============================================
# Database & Caching
# ============================================

MONGODB_URL=mongodb://localhost:27017/
# Local: mongodb://localhost:27017/
# Production (Atlas): mongodb+srv://user:password@cluster.mongodb.net/stratify?retryWrites=true&w=majority
# Get Atlas cluster: https://www.mongodb.com/cloud/atlas (Free M0 tier)

REDIS_URL=redis://localhost:6379
# Local: redis://localhost:6379
# Production (Redis Cloud): redis://default:password@redis-12345.cloud.redislabs.com:12345
# Get Redis Cloud: https://redis.com/try-free/ (Free 30MB tier)

# ============================================
# Security & Authentication
# ============================================

JWT_SECRET_KEY=your-super-secret-jwt-key-minimum-32-characters-change-in-production
# CRITICAL: Change this in production!
# Generate: python -c "import secrets; print(secrets.token_urlsafe(32))"
# Used for: User session tokens

# ============================================
# Server Configuration
# ============================================

PORT=8000
# Backend server port
# Default: 8000
# Production: Usually set by hosting platform (Railway, Render)

RATE_LIMIT_PER_MINUTE=30
# API rate limiting (requests per minute per IP)
# Free tier: 30
# Pro tier: Unlimited (bypass rate limit)

# ============================================
# Feature Flags
# ============================================

CREW_AI_ENABLED=true
# Enable/disable CrewAI agents
# true: Use real AI generation (requires GROQ_API_KEY)
# false: Use demo mock data

FREE_TIER_LIMIT=3
# Number of strategies allowed per month for free users
# Default: 3
# Pro tier: Unlimited

# ============================================
# Production Deployment Notes
# ============================================

# Frontend (Vercel):
# - Build command: npm run build
# - Output directory: dist
# - Environment: Set VITE_API_URL to your backend URL

# Backend (Railway/Render):
# - Start command: uvicorn main:app --host 0.0.0.0 --port $PORT
# - Copy ALL variables above to platform dashboard
# - Set MONGODB_URL and REDIS_URL to cloud instances

# Database (MongoDB Atlas):
# - Create M0 free cluster
# - Network Access: Add 0.0.0.0/0 (allow from anywhere)
# - Database User: Create username/password
# - Copy connection string to MONGODB_URL

# Redis (Redis Cloud):
# - Create free 30MB database
# - Copy connection URL to REDIS_URL

# Stripe Webhook:
# - Production endpoint: https://api.stratify.ai/api/stripe/webhook
# - Test endpoint: Use Stripe CLI for local testing
#   $ stripe listen --forward-to localhost:8000/api/stripe/webhook
